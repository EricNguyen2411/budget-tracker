// Injected by vite.config.ts's `define` at build time — see there for
// how these are actually computed (git commit hash, build timestamp).
declare const __APP_COMMIT__: string
declare const __APP_BUILD_TIME__: string
